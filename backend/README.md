# 午餐點餐平台 - 後端 API

## 技術棧

- NestJS
- TypeScript
- TypeORM
- SQLite (開發環境)
- Zod (資料驗證)

## 安裝

```bash
npm install
```

## 執行

```bash
# 開發模式
npm run dev

# 生產模式
npm run build
npm run start:prod
```

## 環境變數

複製 `.env.example` 為 `.env` 並設定相關變數。

## API 路由結構

所有 API 路由應放在 `src/api/` 資料夾內。
